/laca-occupancy-backend
├── src/
│   ├── controllers/
│   │   └── OccupancyController.js   
│   ├── routes/
│   │   └── OccupancyRoutes.js           ← Express routes for LACA
│   ├── services/
│   │   └── OccupancyService.js          ← Business/data logic
│   ├── config/
│   │   └── db.js                    ← DB connections, site info
│   ├── app.js                               ← Express app setup
│   └── server.js                            ← Main entry point
     |_utils
         |_ doorMap.js         for laca door
      |_timezones.js            




this backend Works for only laca Region 
We have Total 4 Region like APAC , EMEA, LACA, NAMER
so to create new backend we simply update this backend file for EMEA , APAC ,
NAMER....
We need to update controller file,
query Works correct just We need to add Location like 
also We need update database name,
db
multiple timeZone
door list like 

so how to so this make a plan an go step by step 
first if need check my all previous files carefuly then go step by step




